diff --git a/packages/client/ui-agent-preset/src/client/index.ts b/packages/client/ui-agent-preset/src/client/index.ts
index 8b418afe1c..9d27d59b81 100644
--- a/packages/client/ui-agent-preset/src/client/index.ts
+++ b/packages/client/ui-agent-preset/src/client/index.ts
@@ -36,6 +36,13 @@ import { AgentPresetSectionController } from './section-store.ts'
 import { en, zh, type AgentPresetSettingsKey } from './locales.ts'
 import { AGENT_PRESET_SETTINGS_NS, AgentPresetSettingsController } from './settings-store.ts'
 
+declare module '@deepseek-ai/cordis' {
+  interface Context {
+    /** Observable new-session composition selection, shared with target pickers. */
+    uiAgentPreset: Pick<AgentPresetSeatController, 'store' | 'select' | 'stage' | 'load'>
+  }
+}
+
 declare module '@deepseek-ai/dsh-client-ui-slots' {
   interface LocaleNamespaceMap {
     /** Agent-preset surface copy. */
@@ -112,6 +119,7 @@ export function apply(ctx: ClientContext): void {
       return state.current === undefined ? undefined : state.byId[state.current]
     })
     activeSeat = seat
+    scope.effect(() => scope.provide('uiAgentPreset', seat), 'ui-agent-preset: public selection')
     const seatInjected = (): AgentPresetSeatInjected => ({
       hooks: { agentPresetSeat: seat.store },
       load: () => seat.load(),
diff --git a/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx b/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
index 40e08dbaf7..3e80df9fab 100644
--- a/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
+++ b/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
@@ -68,7 +68,7 @@ export const ChatNodeSeat = memo(function ChatNodeSeat({
     && !historyIncomplete
   const processMember = routedNode !== undefined
     && processWindowReady
-    && !TURN_PROCESS_INDEPENDENT_KINDS.has(routedNode.kind)
+    && !(routedNode.independent === true || TURN_PROCESS_INDEPENDENT_KINDS.has(routedNode.kind))
     && routedNode.anchorSeq >= processSpec.processStartSeq
     && routedNode.anchorSeq < processSpec.answerAnchorSeq
   const processAnswer = routedNode !== undefined
diff --git a/packages/client/ui-chat/src/client/contract/chat-nodes.ts b/packages/client/ui-chat/src/client/contract/chat-nodes.ts
index d04bdfd90e..6f8bfa82dd 100644
--- a/packages/client/ui-chat/src/client/contract/chat-nodes.ts
+++ b/packages/client/ui-chat/src/client/contract/chat-nodes.ts
@@ -10,6 +10,8 @@ export interface ChatConversationViewNode extends ConversationViewNode {
   readonly anchorSeq: number
   readonly location: ConversationLocation
   readonly visibility: 'visible' | 'hidden'
+  /** Keep an authored or feature-owned node outside the Turn process disclosure. */
+  readonly independent?: boolean
 }
 
 /** Merge-extensible payload registry keyed by final Chat renderer kind. */
diff --git a/packages/client/ui-chat/src/client/conversation-nodes/chat-snapshot-builder.ts b/packages/client/ui-chat/src/client/conversation-nodes/chat-snapshot-builder.ts
index fb91233786..a446aa8bcd 100644
--- a/packages/client/ui-chat/src/client/conversation-nodes/chat-snapshot-builder.ts
+++ b/packages/client/ui-chat/src/client/conversation-nodes/chat-snapshot-builder.ts
@@ -344,7 +344,7 @@ function turnProcessPresentations(
       })
       continue
     }
-    if (TURN_PROCESS_INDEPENDENT_KINDS.has(node.kind)) continue
+    if ((node.independent === true || TURN_PROCESS_INDEPENDENT_KINDS.has(node.kind))) continue
     presentations.set(location.turn.turn, {
       ...current,
       earliestProcessAnchor: Math.min(current.earliestProcessAnchor ?? node.anchorSeq, node.anchorSeq),
@@ -375,7 +375,7 @@ function presentationPosition(
   const openingHumanAnchor = presentation.openingHumanAnchor
   if (openingHumanAnchor !== undefined
     && node.anchorSeq < openingHumanAnchor
-    && !TURN_PROCESS_INDEPENDENT_KINDS.has(node.kind)) {
+    && !(node.independent === true || TURN_PROCESS_INDEPENDENT_KINDS.has(node.kind))) {
     return { anchor: openingHumanAnchor, rank: 2, originalAnchor: node.anchorSeq }
   }
   if (presentation.control !== undefined && node.key === presentation.control.key) {
diff --git a/packages/client/ui-chat/src/client/conversation-nodes/turn-process-presentation.ts b/packages/client/ui-chat/src/client/conversation-nodes/turn-process-presentation.ts
index a49ae6031b..f2d07f967d 100644
--- a/packages/client/ui-chat/src/client/conversation-nodes/turn-process-presentation.ts
+++ b/packages/client/ui-chat/src/client/conversation-nodes/turn-process-presentation.ts
@@ -54,7 +54,7 @@ function derivePresentation(
       && (spec.answerAnchorSeq === null || node.anchorSeq < spec.answerAnchorSeq)) {
       compactAnswer = false
     }
-    if (TURN_PROCESS_INDEPENDENT_KINDS.has(node.kind)
+    if ((node.independent === true || TURN_PROCESS_INDEPENDENT_KINDS.has(node.kind))
       || node.anchorSeq < spec.processStartSeq
       || (spec.answerAnchorSeq !== null && node.anchorSeq >= spec.answerAnchorSeq)) continue
     if (node.kind !== 'assistant-step' || spec.answerStep === null || node.data.step !== spec.answerStep) {
diff --git a/packages/client/ui-conversation/README.i18n.yaml b/packages/client/ui-conversation/README.i18n.yaml
index 71555a9632..e579bf04ed 100644
--- a/packages/client/ui-conversation/README.i18n.yaml
+++ b/packages/client/ui-conversation/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-conversation/README.md
-README.md: 8d68dfdb61943c7c45b38d9244134edecc86a593
-README.zh.md: b1d548ba8fc9276a6864548802a45796e74c0c7a
+README.md: e32b891146cdd8583870ee405d3d81fb158ef8bb
+README.zh.md: 5d5d16a4d697d0c6ba1aceccc9562f175decdd34
diff --git a/packages/client/ui-conversation/README.md b/packages/client/ui-conversation/README.md
index 8d68dfdb61..e32b891146 100644
--- a/packages/client/ui-conversation/README.md
+++ b/packages/client/ui-conversation/README.md
@@ -107,6 +107,9 @@ try {
 The selector must be a pure function of the owner currency. Its non-null return is delivered to the component as `matched`; `PropsRuntime<'conversation.composer'>` supplies the standard Session and global props. Chain order remains ascending `priority`, then registration order, and the first non-null selector wins. The shell keeps the default composer mounted beneath a takeover. Request state, listeners, response encoding, and any request-specific child slots belong to the business package; they are not carried by `SessionSnapshot` or declared by this core package.
 
 <a id="model-experience"></a>
+
+The Hero exposes a target-selector replacement with the directory selector as fallback, plus an adjacent options slot. Composer blocks are owner-scoped so one plugin cannot clear another plugin’s reason. Clearing a workspace target keeps an existing draft visible and read-only while the composer opens the target picker.
+
 ## Model Experience
 
 None, as this package renders browser state and sends user-admitted inputs through Session Controller APIs without constructing model requests.
diff --git a/packages/client/ui-conversation/README.zh.md b/packages/client/ui-conversation/README.zh.md
index b1d548ba8f..5d5d16a4d6 100644
--- a/packages/client/ui-conversation/README.zh.md
+++ b/packages/client/ui-conversation/README.zh.md
@@ -107,6 +107,9 @@ try {
 selector 必须是 owner currency 的纯函数。非 null 返回值作为 `matched` 传给组件；`PropsRuntime<'conversation.composer'>` 提供标准 Session 与 global props。Chain 顺序仍按 `priority` 升序，再按注册顺序；首个返回非 null 的 selector 获选。Shell 会在 takeover 下保持默认 composer 挂载。Request 状态、listener、response encoding 和任何 request-specific child slot 都属于业务 package，不进入 `SessionSnapshot`，也不由 core 包声明。
 
 <a id="model-experience"></a>
+
+Hero 提供目标选择器替换位置，以文件夹选择器为默认显示，并提供相邻选项位置。输入阻止理由按贡献者区分，插件不能清除其他插件的理由。清除工作区选择后，已有输入保持可见并只读，点击输入框仍可打开目标选择器。
+
 ## 模型体验
 
 无，因为本包渲染浏览器状态，并通过 Session Controller API 发送用户确认提交的输入，而不构造模型请求。
diff --git a/packages/client/ui-conversation/src/client/apply.ts b/packages/client/ui-conversation/src/client/apply.ts
index 95e6c5f3bc..afa7014159 100644
--- a/packages/client/ui-conversation/src/client/apply.ts
+++ b/packages/client/ui-conversation/src/client/apply.ts
@@ -250,6 +250,8 @@ export function apply(ctx: Context, config: Config = Config({})): void {
       'conversation.input.dock': { kind: 'list', scope: 'session' },
       'conversation.hero.brand.mark': { kind: 'single', scope: 'root' },
       'conversation.hero.workspace': { kind: 'single', scope: 'root' },
+      'conversation.hero.target': { kind: 'single', scope: 'root' },
+      'conversation.hero.options': { kind: 'list', scope: 'root' },
       'conversation.hero.agentPreset': { kind: 'single', scope: 'root' },
     },
     inject: (sessionId: SessionId | undefined): ConversationInjected => ({
diff --git a/packages/client/ui-conversation/src/client/contract/composer-blocks.ts b/packages/client/ui-conversation/src/client/contract/composer-blocks.ts
index bf25624b04..277174547a 100644
--- a/packages/client/ui-conversation/src/client/contract/composer-blocks.ts
+++ b/packages/client/ui-conversation/src/client/contract/composer-blocks.ts
@@ -13,8 +13,9 @@ export interface ComposerBlocks {
    * Raise or clear this session's block.
    * @param sessionId - Session whose composer is affected.
    * @param block - Block to raise, or undefined to clear it.
+   * @param owner - Independent producer identity; omitted addresses the default producer.
    */
-  set(sessionId: SessionId, block: ComposerBlock | undefined): void
+  set(sessionId: SessionId, block: ComposerBlock | undefined, owner?: string): void
   /**
    * Resolve the observable block state for one Session.
    * @param sessionId - Session to observe.
diff --git a/packages/client/ui-conversation/src/client/contract/records.ts b/packages/client/ui-conversation/src/client/contract/records.ts
index 0e61abe4b0..6852ed6546 100644
--- a/packages/client/ui-conversation/src/client/contract/records.ts
+++ b/packages/client/ui-conversation/src/client/contract/records.ts
@@ -60,6 +60,8 @@ export interface AssistantTiming {
 /** A finalized assistant message or an interruption-frozen streaming prefix. */
 export interface AssistantMessageNode {
   kind: 'assistant'
+  /** Authored message provenance; absent on model-generated settlements. */
+  source?: unknown
   seq: number
   /**
    * Stable identity carried from the `assistant/message` event. Absent only on
diff --git a/packages/client/ui-conversation/src/client/contract/slots.ts b/packages/client/ui-conversation/src/client/contract/slots.ts
index 795090f465..c1c80b2989 100644
--- a/packages/client/ui-conversation/src/client/contract/slots.ts
+++ b/packages/client/ui-conversation/src/client/contract/slots.ts
@@ -160,6 +160,10 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
     'conversation.hero.workspace': { kind: 'single'; scope: 'root'; owner: EmptyWorkspaceOwnerProps }
     /** Brand mark shown before the blank-session headline. */
     'conversation.hero.brand.mark': { kind: 'single'; scope: 'root'; owner: HeroBrandMarkOwnerProps }
+    /** Resource-specific target selector; the owner supplies the normal directory selector. */
+    'conversation.hero.target': { kind: 'single'; scope: 'root'; owner: { folderPicker: ReactNode; pickerOpen: boolean; setPickerOpen: (open: boolean) => void; selectWorkspace: (id: import('@deepseek-ai/dsh-api-workspace-controller/client').WorkspaceId) => Promise<void> } }
+    /** Additional preparation controls beside target and mode. */
+    'conversation.hero.options': { kind: 'list'; scope: 'root' }
     /** Agent-preset control staged for a New Session. */
     'conversation.hero.agentPreset': { kind: 'single'; scope: 'root'; owner: HeroAgentPresetOwnerProps }
     /** Full-width entries above the composer card. */
@@ -372,7 +376,7 @@ export type ConversationSlotProps =
     | 'conversation.input.dock'
     | 'conversation.hero.brand.mark'
     | 'conversation.hero.workspace'
-    | 'conversation.hero.agentPreset'
+    | 'conversation.hero.agentPreset' | 'conversation.hero.target' | 'conversation.hero.options'
   >
   & InjectFace<ConversationInjected>
   & PropsLocale<'conversation'>
diff --git a/packages/client/ui-conversation/src/client/input/blocks.ts b/packages/client/ui-conversation/src/client/input/blocks.ts
index 692b808f15..403a70a739 100644
--- a/packages/client/ui-conversation/src/client/input/blocks.ts
+++ b/packages/client/ui-conversation/src/client/input/blocks.ts
@@ -20,8 +20,16 @@ import type { ComposerBlock, ComposerBlocks } from '../contract/composer-blocks.
 export class ComposerBlockRegistry implements ComposerBlocks {
   private readonly stores = new Map<SessionId, SnapshotStore<ComposerBlock | undefined>>()
 
+  private readonly owners = new Map<SessionId, Map<string, ComposerBlock>>()
+
   /** @inheritdoc */
-  set(sessionId: SessionId, block: ComposerBlock | undefined): void {
+  set(sessionId: SessionId, block: ComposerBlock | undefined, owner = 'default'): void {
+    const owners = this.owners.get(sessionId) ?? new Map<string, ComposerBlock>()
+    if (block === undefined) owners.delete(owner)
+    else owners.set(owner, block)
+    if (owners.size > 0) this.owners.set(sessionId, owners)
+    else this.owners.delete(sessionId)
+    block = owners.values().next().value
     const store = this.storeFor(sessionId)
     const current = store.getSnapshot()
     if (current?.reason === block?.reason) return
@@ -39,6 +47,7 @@ export class ComposerBlockRegistry implements ComposerBlocks {
 
   /** @inheritdoc */
   forget(sessionId: SessionId): void {
+    this.owners.delete(sessionId)
     this.stores.delete(sessionId)
   }
 }
diff --git a/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.tsx b/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.tsx
index dfb515bde6..1fa28c0284 100644
--- a/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.tsx
+++ b/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.tsx
@@ -290,29 +290,33 @@ export function ConversationRoot({
           ? undefined
           : workspaceLabel(cwd)))
 
+  const folderPicker = <>
+    <WorkspaceChip
+      buttonRef={pickerAnchor}
+      label={chipTitle}
+      menuOpen={pickerOpen}
+      onClick={() => { setPickerOpen(open => !open) }}
+      t={t}
+    />
+    {renderSlot('conversation.hero.workspace', {
+      open: pickerOpen,
+      anchorRef: pickerAnchor,
+      selectedId: pendingWorkspaceId ?? sessionWorkspace?.workspaceId,
+      onPick: (workspaceId) => {
+        setPickerOpen(false)
+        setPendingWorkspaceId(workspaceId)
+        void selectWorkspace(workspaceId).catch(() => {
+          setPendingWorkspaceId(current => current === workspaceId ? undefined : current)
+        })
+      },
+      onClose: () => { setPickerOpen(false) },
+    })}
+  </>
   const heroWorkspaceRow = (
     <div className={css.heroWorkspaceRow}>
-      <WorkspaceChip
-        buttonRef={pickerAnchor}
-        label={chipTitle}
-        menuOpen={pickerOpen}
-        onClick={() => { setPickerOpen(open => !open) }}
-        t={t}
-      />
-      {renderSlot('conversation.hero.workspace', {
-        open: pickerOpen,
-        anchorRef: pickerAnchor,
-        selectedId: pendingWorkspaceId ?? sessionWorkspace?.workspaceId,
-        onPick: (workspaceId) => {
-          setPickerOpen(false)
-          setPendingWorkspaceId(workspaceId)
-          void selectWorkspace(workspaceId).catch(() => {
-            setPendingWorkspaceId(current => current === workspaceId ? undefined : current)
-          })
-        },
-        onClose: () => { setPickerOpen(false) },
-      })}
+      {renderSlot('conversation.hero.target', { folderPicker, pickerOpen, setPickerOpen, selectWorkspace }, { fallback: folderPicker })}
       {renderSlot('conversation.hero.agentPreset', {})}
+      {renderSlot('conversation.hero.options', {})}
     </div>
   )
 
diff --git a/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx b/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx
index 6b7d83f3f6..c8da3fc358 100644
--- a/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx
+++ b/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx
@@ -461,7 +461,7 @@ export const InputBar = memo(function InputBar({
         <div ref={scrollRef} className={css.scroll} data-input-scroll>
           <div className={css.grow}>
             <ComposerContentEditable
-              editor={workspaceTrigger ? null : editor}
+              editor={editor}
               editable={editable}
               className={clsx(css.input, editorDisabled && css.inputDisabled)}
               data-phase={input?.phase ?? 'inert'}
@@ -481,7 +481,7 @@ export const InputBar = memo(function InputBar({
                 {placeholderText}
               </div>
             )}
-            <DecoratorPortals editor={workspaceTrigger ? null : editor} />
+            <DecoratorPortals editor={editor} />
           </div>
         </div>
         <div className={css.row}>
diff --git a/packages/client/ui-conversation/tests/composer-blocks.client.spec.ts b/packages/client/ui-conversation/tests/composer-blocks.client.spec.ts
new file mode 100644
index 0000000000..4ded681334
--- /dev/null
+++ b/packages/client/ui-conversation/tests/composer-blocks.client.spec.ts
@@ -0,0 +1,15 @@
+import { expect, it } from 'vitest'
+import { SessionId } from '@deepseek-ai/dsh-session/types'
+import { ComposerBlockRegistry } from '../src/client/input/blocks.ts'
+it('keeps independent blockers until each owner releases its own entry', () => {
+  const blocks = new ComposerBlockRegistry(), id = SessionId('fixture')
+  blocks.set(id, { reason: 'model' })
+  blocks.set(id, { reason: 'configuration' }, 'configuration')
+  blocks.set(id, undefined)
+  expect(blocks.storeFor(id).getSnapshot()).toEqual({ reason: 'configuration' })
+  blocks.set(id, undefined, 'configuration')
+  expect(blocks.storeFor(id).getSnapshot()).toBeUndefined()
+  blocks.set(id, { reason: 'again' }, 'configuration')
+  blocks.forget(id)
+  expect(blocks.storeFor(id).getSnapshot()).toBeUndefined()
+})
diff --git a/packages/client/ui-conversation/tests/input-bar.client.spec.tsx b/packages/client/ui-conversation/tests/input-bar.client.spec.tsx
index 13266b1ca2..1ca1c53ab7 100644
--- a/packages/client/ui-conversation/tests/input-bar.client.spec.tsx
+++ b/packages/client/ui-conversation/tests/input-bar.client.spec.tsx
@@ -1215,6 +1215,21 @@ describe('running and lock semantics', () => {
     expect(custom.placeholder).toBe('Custom placeholder')
   })
 
+  it('keeps an existing draft visible and readonly while the workspace target is cleared', () => {
+    const { view, props, shell, textarea } = bench()
+    writeDraft(shell, 'Unsent instruction')
+    const onRequestWorkspace = vi.fn()
+    view.rerender(<InputBar {...props} disabled onRequestWorkspace={onRequestWorkspace} />)
+    expect(textarea.textContent).toBe('Unsent instruction')
+    expect(editableOf(textarea)).toBe(false)
+    fireEvent.click(textarea)
+    expect(onRequestWorkspace).toHaveBeenCalledOnce()
+    view.rerender(<InputBar {...props} />)
+    expect(textarea.textContent).toBe('Unsent instruction')
+    expect(shell.snapshot.draft).toBe('Unsent instruction')
+    expect(editableOf(textarea)).toBe(true)
+  })
+
   it('the inert textarea opens the Workspace picker by pointer or keyboard', () => {
     const onRequestWorkspace = vi.fn()
     const { view, textarea } = bench({
diff --git a/packages/client/ui-trajectory/src/client/layout.ts b/packages/client/ui-trajectory/src/client/layout.ts
index fd361899d0..644e32025d 100644
--- a/packages/client/ui-trajectory/src/client/layout.ts
+++ b/packages/client/ui-trajectory/src/client/layout.ts
@@ -731,7 +731,8 @@ function expandAssistant(
     .join('\n\n')
   const message: TrajectoryCellProps = {
     index: ++index,
-    recordId: `assistant\u0000${node.turn}\u0000${node.step}`,
+    recordId: node.source === undefined ? `assistant\u0000${node.turn}\u0000${node.step}` : `authored\u0000${node.seq}`,
+    ...(node.source === undefined ? {} : { messageSource: node.source }),
     kind: 'message',
     sourceSeq: node.seq,
     text: messageText !== '' || thinkingText !== ''
@@ -745,11 +746,11 @@ function expandAssistant(
     ...(messageText !== '' ? { outputDetail: messageText } : {}),
     ...(thinkingText !== '' ? { thinkingDetail: thinkingText } : {}),
     sourceBlocks: node.blocks.map(block => assistantSourceBlock(block)),
-    timeSeconds: messageDuration,
+    timeSeconds: node.source === undefined ? messageDuration : 0,
     startedAt: recordedStart,
   }
   attachUsage(message, usage)
-  message.assistantMetrics = {
+  if (node.source === undefined) message.assistantMetrics = {
     timingRecorded: node.timing !== undefined,
     stepStartTime: node.timing?.stepStartTime ?? null,
     firstTokenTime: node.timing?.firstTokenTime ?? null,
diff --git a/packages/client/ui-trajectory/src/client/trajectory-message-definitions.ts b/packages/client/ui-trajectory/src/client/trajectory-message-definitions.ts
index deb4097406..97451f0ba2 100644
--- a/packages/client/ui-trajectory/src/client/trajectory-message-definitions.ts
+++ b/packages/client/ui-trajectory/src/client/trajectory-message-definitions.ts
@@ -189,4 +189,22 @@ const trajectoryMessageDefinition: ConversationNodeDefinition<MessageNode> = {
 export function registerTrajectoryMessageDefinitions(ctx: Context): void {
   ctx.uiConversation.events.register(trajectoryInboxDefinition)
   ctx.uiConversation.events.register(trajectoryMessageDefinition)
+  ctx.uiConversation.events.register({
+    kind: 'trajectory-authored-message', target: 'trajectory',
+    match: event => event.type === 'context/message' ? { id: String(event.seq), role: 'start' } : null,
+    start: () => true,
+    update: () => true,
+    buildViewNode: (context) => {
+      const match = context.start
+      if (match?.event.type !== 'context/message') return null
+      const event = match.event, { message, name, groupId } = event.data
+      const source = { ...message.source, name, groupId, authored: true }
+      const node = message.role === 'user'
+        ? { kind: 'user' as const, seq: event.seq, time: event.time, content: message.content, source }
+        : { kind: 'assistant' as const, messageId: message.id, seq: event.seq, time: event.time,
+          turn: match.location.kind === 'step' ? match.location.turn.turn : 0, step: 0, source,
+          blocks: message.content.map(block => ({ kind: 'text' as const, text: block.type === 'text' ? block.text : '' })) }
+      return trajectoryNode(context, event.seq, { kind: 'node', node })
+    },
+  })
 }
diff --git a/packages/client/ui-workspace/src/client/WorkspacePicker.tsx b/packages/client/ui-workspace/src/client/WorkspacePicker.tsx
index b1801cc3d9..6126220d22 100644
--- a/packages/client/ui-workspace/src/client/WorkspacePicker.tsx
+++ b/packages/client/ui-workspace/src/client/WorkspacePicker.tsx
@@ -70,7 +70,7 @@ export function WorkspacePickFlow({
   selectedId,
 }: WorkspacePickFlowProps) {
   const workspaceSnapshot = useWorkspaces(state => state)
-  const workspaces = workspaceSnapshot.items
+  const workspaces = workspaceSnapshot.items.filter(workspace => workspace.target === undefined)
   const getAnchorRect = useCallback(
     () => anchorRef?.current?.getBoundingClientRect() ?? null,
     [anchorRef],
diff --git a/packages/client/ui-workspace/src/client/navigation.ts b/packages/client/ui-workspace/src/client/navigation.ts
index c968595d8a..6c343928f5 100644
--- a/packages/client/ui-workspace/src/client/navigation.ts
+++ b/packages/client/ui-workspace/src/client/navigation.ts
@@ -120,7 +120,7 @@ class UiWorkspaceService extends Service implements UiWorkspace {
     const sessions = this.sessions.list.getSnapshot()
     for (const id of sessions.ids) {
       const summary = sessions.byId[id]
-      if (summary !== undefined && summary.blank && summary.cwd === workspace.path
+      if (summary !== undefined && summary.blank && (workspace.target !== undefined || summary.cwd === workspace.path)
         && workspace.sessionIds.includes(summary.id)
         && !archived.includes(summary.id)) return summary.id
     }
diff --git a/packages/client/ui-workspace/src/client/rows/Rows.tsx b/packages/client/ui-workspace/src/client/rows/Rows.tsx
index 0c2101bd0a..5456b8f615 100644
--- a/packages/client/ui-workspace/src/client/rows/Rows.tsx
+++ b/packages/client/ui-workspace/src/client/rows/Rows.tsx
@@ -9,7 +9,7 @@ import { useEffect, useRef, useState } from 'react'
 import clsx from 'clsx'
 import {
   HoverCard, IconAlarmClockOutline16, IconArchiveOutline20, IconBranchOutline16,
-  IconEditOutline16, IconEllipsisOutline16, IconFolderClose16, IconFolderOpen16,
+  IconEditOutline16, IconEllipsisOutline16, IconBranchOutline16 as IconResource16, IconFolderClose16, IconFolderOpen16,
   IconPlusOutline16, IconTrashOutline16, IconTriangleRightFill14, Menu, relativeTime,
   StateDot,
 } from '@deepseek-ai/dsh-client-ui-primitives'
@@ -147,7 +147,7 @@ export function ProjectRowItem({ group, onToggle, onCreate, actions, drag, home,
       onDragEnd={drag?.end}
     >
       <span className={clsx(css.slot, css.folder, active && css.folderActive)}>
-        {row.expanded ? <IconFolderOpen16 /> : <IconFolderClose16 />}
+        {row.target !== undefined ? <IconResource16 /> : row.expanded ? <IconFolderOpen16 /> : <IconFolderClose16 />}
       </span>
       <span className={clsx(css.slot, css.chevron)}>
         <IconTriangleRightFill14 className={clsx(css.arrow, row.expanded && css.arrowOpen)} />
diff --git a/packages/client/ui-workspace/src/client/tree.ts b/packages/client/ui-workspace/src/client/tree.ts
index 37c6d50e38..2e92965a59 100644
--- a/packages/client/ui-workspace/src/client/tree.ts
+++ b/packages/client/ui-workspace/src/client/tree.ts
@@ -67,6 +67,7 @@ export interface GroupNode {
   /** Backing Workspace id; absent only for the ungrouped bucket. */
   workspaceId: WorkspaceId | undefined
   cwd: string | undefined
+  target?: WorkspaceView['target']
   /** Workspace creation time (epoch ms); absent only for the ungrouped bucket. */
   createdAt: number | undefined
   label: string
@@ -113,6 +114,7 @@ interface Group {
   key: string
   workspaceId: WorkspaceId | undefined
   cwd: string | undefined
+  target?: WorkspaceView['target']
   createdAt: number | undefined
   label: string
   sessions: SessionSummary[]
@@ -220,10 +222,10 @@ function groupByWorkspace(
       if (!sessionVisible(summary, list.current, archived)) continue
       members.push(summary)
     }
-    groups.push(buildGroup(
-      workspace.workspaceId, workspace.workspaceId, workspace.path,
+    groups.push({ ...buildGroup(
+      workspace.workspaceId, workspace.workspaceId, workspace.target === undefined ? workspace.path : undefined,
       Date.parse(workspace.createdAt), workspace.title, members, 'account',
-    ))
+    ), ...(workspace.target === undefined ? {} : { target: workspace.target }) })
   }
   const stray = list.ids
     .map(id => list.byId[id])
@@ -309,6 +311,7 @@ export function deriveGroups(
       key: g.key,
       workspaceId: g.workspaceId,
       cwd: g.cwd,
+      ...(g.target === undefined ? {} : { target: g.target }),
       createdAt: g.createdAt,
       label: g.label,
       sessionCount: g.sessions.length,
diff --git a/packages/extensions/cordis-client-runner/src/client/api-catalog.ts b/packages/extensions/cordis-client-runner/src/client/api-catalog.ts
index dfc66c56bf..edcee4be43 100644
--- a/packages/extensions/cordis-client-runner/src/client/api-catalog.ts
+++ b/packages/extensions/cordis-client-runner/src/client/api-catalog.ts
@@ -916,7 +916,7 @@ export const TYPE_API: readonly TypeApiEntry[] = [
   },
   {
     name: 'WorkspaceView',
-    declaration: 'export interface WorkspaceView {\n    readonly workspaceId: WorkspaceId;\n    readonly path: string;\n    readonly title: string;\n    readonly sessionIds: readonly SessionId[];\n    readonly createdAt: string;\n    readonly updatedAt: string;\n}',
+    declaration: 'export interface WorkspaceView {\n    readonly workspaceId: WorkspaceId;\n    readonly target?: {\n        readonly provider: string;\n        readonly key: string;\n    };\n    readonly path: string;\n    readonly title: string;\n    readonly sessionIds: readonly SessionId[];\n    readonly createdAt: string;\n    readonly updatedAt: string;\n}',
   },
 ]
 
diff --git a/packages/extensions/cordis-client-runner/src/client/slot-catalog.ts b/packages/extensions/cordis-client-runner/src/client/slot-catalog.ts
index cebcefaada..a87e019d27 100644
--- a/packages/extensions/cordis-client-runner/src/client/slot-catalog.ts
+++ b/packages/extensions/cordis-client-runner/src/client/slot-catalog.ts
@@ -425,7 +425,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'shadows-shipped-ui',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.composer.bar\', () => ctx.slots.register(\n      { name: \'conversation.composer.bar\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:176',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:180',
   },
   {
     key: 'conversation.composer.dock',
@@ -480,7 +480,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.composer.dock\', () => ctx.slots.register(\n      { name: \'conversation.composer.dock\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:170',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:174',
   },
   {
     key: 'conversation.hero.agentPreset',
@@ -510,7 +510,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'shadows-shipped-ui',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.hero.agentPreset\', () => ctx.slots.register(\n      { name: \'conversation.hero.agentPreset\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:164',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:168',
   },
   {
     key: 'conversation.hero.brand.mark',
@@ -540,6 +540,77 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.hero.brand.mark\', () => ctx.slots.register(\n      { name: \'conversation.hero.brand.mark\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
     source: 'packages/client/ui-conversation/src/client/contract/slots.ts:162',
   },
+  {
+    key: 'conversation.hero.options',
+    kind: 'list',
+    scope: 'root',
+    summary: 'Additional preparation controls beside target and mode.',
+    doc: 'Additional preparation controls beside target and mode.',
+    registerOptions: [
+      {
+        name: 'id',
+        requirement: 'required',
+        type: 'string',
+        doc: 'Your cell key. Use an id of your own: a fresh id is added beside the shipped entries, while reusing a shipped id puts you in THAT cell and replaces it. Owners that filter by id address you by it.',
+      },
+      {
+        name: 'order',
+        requirement: 'optional',
+        type: 'number',
+        doc: 'Position among the entries, ascending (default 0).',
+      },
+      {
+        name: 'label',
+        requirement: 'optional',
+        type: 'string | (() => string)',
+        doc: 'Display text where the owner projects one (nav rows, tabs). A thunk is re-read on every projection, so localized text follows the active locale without re-registering.',
+      },
+    ],
+    ownerProps: [],
+    ownerPropsReferences: [],
+    standardProps: [
+      'useResource: UseResource',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+      'usePanelInfo: UsePanelInfo',
+      'useSessions: UseSessions',
+      'useSessionPendingInteraction: UseSessionPendingInteraction',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+    ],
+    keyDomain: '',
+    hookContext: '',
+    slotInject: '',
+    declaredBy: 'an entry in \'main.conversation\' (client-ui-conversation), so it exists while that entry is mounted',
+    occupants: [],
+    replaceRisk: 'none',
+    example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.hero.options\', () => ctx.slots.register(\n      { name: \'conversation.hero.options\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:166',
+  },
+  {
+    key: 'conversation.hero.target',
+    kind: 'single',
+    scope: 'root',
+    summary: 'Resource-specific target selector; the owner supplies the normal directory selector.',
+    doc: 'Resource-specific target selector; the owner supplies the normal directory selector.',
+    registerOptions: [],
+    ownerProps: [],
+    ownerPropsReferences: [],
+    standardProps: [
+      'useResource: UseResource',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+      'usePanelInfo: UsePanelInfo',
+      'useSessions: UseSessions',
+      'useSessionPendingInteraction: UseSessionPendingInteraction',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+    ],
+    keyDomain: '',
+    hookContext: '',
+    slotInject: '',
+    declaredBy: 'an entry in \'main.conversation\' (client-ui-conversation), so it exists while that entry is mounted',
+    occupants: [],
+    replaceRisk: 'none',
+    example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.hero.target\', () => ctx.slots.register(\n      { name: \'conversation.hero.target\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:164',
+  },
   {
     key: 'conversation.hero.workspace',
     kind: 'single',
@@ -641,7 +712,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'shadows-shipped-ui',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.input.attachments\', () => ctx.slots.register(\n      { name: \'conversation.input.attachments\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:178',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:182',
   },
   {
     key: 'conversation.input.dock',
@@ -703,7 +774,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.input.dock\', () => ctx.slots.register(\n      { name: \'conversation.input.dock\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:166',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:170',
   },
   {
     key: 'conversation.input.left',
@@ -756,7 +827,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     occupants: [],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.input.left\', () => ctx.slots.register(\n      { name: \'conversation.input.left\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:172',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:176',
   },
   {
     key: 'conversation.input.model',
@@ -794,7 +865,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'shadows-shipped-ui',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.input.model\', () => ctx.slots.register(\n      { name: \'conversation.input.model\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:186',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:190',
   },
   {
     key: 'conversation.input.overlay',
@@ -851,7 +922,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.input.overlay\', () => ctx.slots.register(\n      { name: \'conversation.input.overlay\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:168',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:172',
   },
   {
     key: 'conversation.input.plan',
@@ -889,7 +960,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'shadows-shipped-ui',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.input.plan\', () => ctx.slots.register(\n      { name: \'conversation.input.plan\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:184',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:188',
   },
   {
     key: 'conversation.input.right',
@@ -942,7 +1013,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     occupants: [],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.input.right\', () => ctx.slots.register(\n      { name: \'conversation.input.right\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:174',
+    source: 'packages/client/ui-conversation/src/client/contract/slots.ts:178',
   },
   {
     key: 'conversation.message.images',
