- insert:
    - id: writer-fixture
      name: './adapter.mjs'
- id: agent-default-model
  config:
    provider: writer-fixture
    model: fixture
- id: session-query-sqlite
  config:
    openAt: first-search
    path: ":memory:"
